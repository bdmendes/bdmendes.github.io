---
import { Icon } from "astro-icon/components";
import { SOCIALS } from "../../config";
---

<div class="py-5 flex justify-center w-full gap-4">
  {
    Object.entries(SOCIALS).map(([key, value]) => (
      <a
        href={value.url}
        target="_blank"
        aria-label={key}
        title={key}
      >
        <Icon name={value.icon} class="hover:shadow-xl" size="24" />
      </a>
    ))
  }
</div>
